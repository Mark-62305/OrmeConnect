<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#FFFFFF"
    android:padding="12dp"
    android:layout_marginTop="8dp">

    <!-- Top row: title + status pill -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical">

        <!-- Incident Title / Category (e.g. Outage Report) -->
        <TextView
            android:id="@+id/tvIncidentTitle"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Outage Report"
            android:textSize="14sp"
            android:textStyle="bold" />

        <!-- Status (e.g. Under Inspection / Resolved) -->
        <TextView
            android:id="@+id/tvIncidentStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Under Inspection"
            android:textSize="12sp"
            android:paddingLeft="8dp"
            android:paddingRight="8dp"
            android:paddingTop="4dp"
            android:paddingBottom="4dp"
            android:background="#FEF3C7" />
    </LinearLayout>

    <!-- Location / Short description -->
    <TextView
        android:id="@+id/tvIncidentLocation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="123 Sample St"
        android:layout_marginTop="4dp"
        android:textSize="13sp" />

    <!-- Date / reported_at -->
    <TextView
        android:id="@+id/tvIncidentDate"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Reported: 2025-11-27 10:30"
        android:textSize="12sp"
        android:textColor="#666666"
        android:layout_marginTop="2dp" />

</LinearLayout>
