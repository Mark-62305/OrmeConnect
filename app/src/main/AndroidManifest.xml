<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Location permissions for GPS coordinate capture -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application android:name=".MyApp"
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:theme="@style/Theme.MyApplication">

        <!--
            IMPORTANT: Replace YOUR_GOOGLE_MAPS_API_KEY below with a real key.
            Get one at: https://console.cloud.google.com → APIs & Services → Maps SDK for Android
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_GOOGLE_MAPS_API_KEY" />



        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".QrLoginActivity"/>
        <activity android:name=".VisitorCalculatorActivity" />
        <activity android:name=".DashboardVisitorActivity" />
        <activity android:name=".CreateAccountActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".PredictActivity" />
        <activity android:name=".IncidentReportsActivity" />
        <activity android:name=".BenefitsActivity" />
        <activity android:name=".BillingCalculatorActivity" />
        <activity android:name=".MyMetersActivity" />
        <activity android:name=".MeterHistoryActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".SchedulingActivity" />


    </application>

</manifest>